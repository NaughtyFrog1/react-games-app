const path = require('path')

const DIR_REVERSI = path.join(__dirname, '..', 'data', 'reversi.json')
const DIR_PLAYERS = path.join(__dirname, '..', 'data', 'players.json')

module.exports = { DIR_REVERSI, DIR_PLAYERS }
